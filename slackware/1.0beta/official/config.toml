# Declarative configuration for the Python retro host.

[download]
slackware_mirror = "pre-1.0-beta"

[extract]
custom_script = "extract.sh"
custom_source = "packages"
boot_image = "a1/a1disk"
package_source = "."
package_dest = "install"
