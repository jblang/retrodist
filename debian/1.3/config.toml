# Declarative configuration for the Python retro host.

[install]
driver = "debian-dinstall"

[qemu]
profile = "linux-2.0"

[qemu.network]
device = "pcnet"

[install.network]
hostname = "bo"

[install.debian]
driver_floppy = false
relogin = true

[postinst]
stages = ["tty"]
