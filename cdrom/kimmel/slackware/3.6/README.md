# Slackware 3.6

The available Kimmel `disc1.iso` appears to be corrupted.

`7z x disc1.iso` reports `Unexpected end of archive` and fails on
`zipslack/zipslack.zip`.

This limitation applies to the image downloaded from this directory's
`config.toml`; use another Slackware 3.6 media variant for installation.
